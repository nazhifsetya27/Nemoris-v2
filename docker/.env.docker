# Docker-specific overrides
# Used when running full stack via: docker compose -f docker/docker-compose.yml --profile all up -d

DATABASE_URL=postgresql://nemoris:nemoris123@nemoris_db:5432/nemoris
CHROMA_URL=http://nemoris_chroma:8000
WAHA_URL=http://nemoris_waha:3000
# LLM runs on host - start with: npm run proxy
LLM_URL=http://host.docker.internal:4096
